language: node_js
env:
  - CXX=g++-4.8
node_js:
  - 6.9
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - g++-4.8
before_install:
  - "pushd $PWD"
  - "cd /tmp"
  - "wget http://github.com/wojtekka/libgadu/releases/download/1.12.1/libgadu-1.12.1.tar.gz"
  - "tar -zxvf libgadu-1.12.1.tar.gz"
  - "cd libgadu-1.12.1"
  - "./configure --prefix=/usr"
  - "make"
  - "sudo make install"
  - "popd"
